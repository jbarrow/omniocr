[project]
name = "anyocr"
version = "0.1.0"
description = "Run ~any open source OCR model behind a single API."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.12.5",
    "pytest>=9.0.2",
]
